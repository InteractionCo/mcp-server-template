services:
  - type: web
    name: github-poke-bridge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.server
    plan: free
    autoDeploy: false
    envVars:
      - key: ENVIRONMENT
        value: production
